package logic.objects.message.types;

public enum ResponseType{
    OK,
    LOGIN_CREDENTIAL_ERROR,
    SERVER_ERROR,
    DATABASE_ERROR,
    USER_ERROR,
    EMAIL_EXISTS_ERROR,
    LOGIN_EXISTS_ERROR
}
